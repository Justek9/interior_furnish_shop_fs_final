import NavBar from '../NavBar/NavBar';
import TopBar from '../TopBar/TopBar';

const Header = () => {
  return (
    <div>
      <TopBar />
      <NavBar />
    </div>
  );
};

export default Header;
